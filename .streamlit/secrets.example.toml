# Deprecated for the Streamlit Cloud app UI.
# Cloud uses repo-published S2 lookup files and does not expose S2 login.
# Keep real credentials out of git. Local/admin refresh should use .env instead.

S2_API_BASE_URL = "https://kiss-api.kld.kr"
S2_ID = ""
S2_PW = ""
# Optional: if set, S2 refresh skips the login request and uses this bearer token.
S2_ACCESS_TOKEN = ""

# Optional: enables the sidebar "관리자에게 S2 최신화 요청" button.
# Store real values only in Streamlit Cloud Secrets, never in git.
[clickup]
api_token = ""
list_id = ""
# Optional: if blank, the app asks ClickUp who owns the token and assigns the task to that user.
assignee_ids = ""
api_base_url = "https://api.clickup.com/api/v2"
app_url = "https://mapping-novel-ascmdzm897irzyvzwn9kqo.streamlit.app/"
adapter_failure_list_id = "901818576269"
adapter_failure_assignee_ids = "306885786"
adapter_failure_status = "to do"
adapter_failure_priority = "1"
adapter_failure_due_date_minutes = "2"
adapter_failure_attach_original = "true"
adapter_failure_tags = "adapter-failure,mapping-novel"

# Optional: creates sanitized GitHub Issues for adapter failure tracking.
[github]
adapter_failure_token = ""
adapter_failure_repo = "macximin/mapping-novel"
adapter_failure_assignees = ""
adapter_failure_mentions = ""
adapter_failure_labels = "adapter-failure,urgent"
